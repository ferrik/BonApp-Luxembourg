Use MASTER_PROMPT.

Task: Build MVP UX flow.

Flow:
Home -> Category -> 3 results -> Restaurant detail -> CTA

Requirements:
- English + French UI text
- category buttons
- max 3 restaurant cards
- detail page
- CTA buttons for order, call, website
- tracking wrapper
- mobile-first

Do not build login, payments, cart, courier logic, or menu management.
